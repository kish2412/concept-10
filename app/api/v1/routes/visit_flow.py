"""
Visit flow routes.

Note: Models live in app.models.visit_flow. This module intentionally
defines only API routes to avoid duplicate table registration.
"""
from fastapi import APIRouter

router = APIRouter()

